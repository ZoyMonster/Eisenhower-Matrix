# DMG 和应用图标制作说明

## 图标尺寸要求

### 源图片要求
- **推荐尺寸**：1024×1024 像素（正方形）
- **最小尺寸**：512×512 像素
- **格式**：PNG（透明背景）或 JPG
- **设计建议**：图标应该简洁清晰，在小尺寸下也能识别

### .icns 文件包含的尺寸
macOS 的 `.icns` 文件需要包含以下尺寸（系统会自动生成）：
- 16×16 像素
- 32×32 像素
- 48×48 像素
- 128×128 像素
- 256×256 像素
- 512×512 像素
- 1024×1024 像素（Retina 显示）

## 制作步骤

### 方法一：使用在线工具（推荐）
1. 准备一个 1024×1024 的 PNG 图片
2. 访问在线转换工具：
   - https://cloudconvert.com/png-to-icns
   - https://iconverticons.com/online/
3. 上传 PNG 图片，下载生成的 .icns 文件
4. 将文件保存为 `build/icon.icns`

### 方法二：使用 macOS 自带工具
1. 准备一个 1024×1024 的 PNG 图片
2. 打开"预览"应用，打开你的 PNG 图片
3. 选择"文件" → "导出"
4. 格式选择"ICNS"，保存为 `build/icon.icns`

### 方法三：使用命令行工具（需要安装 iconutil）
```bash
# 创建 iconset 目录结构
mkdir icon.iconset

# 使用 sips 工具生成不同尺寸（需要准备 1024x1024 的 icon.png）
sips -z 16 16     icon.png --out icon.iconset/icon_16x16.png
sips -z 32 32     icon.png --out icon.iconset/icon_16x16@2x.png
sips -z 32 32     icon.png --out icon.iconset/icon_32x32.png
sips -z 64 64     icon.png --out icon.iconset/icon_32x32@2x.png
sips -z 128 128   icon.png --out icon.iconset/icon_128x128.png
sips -z 256 256   icon.png --out icon.iconset/icon_128x128@2x.png
sips -z 256 256   icon.png --out icon.iconset/icon_256x256.png
sips -z 512 512   icon.png --out icon.iconset/icon_256x256@2x.png
sips -z 512 512   icon.png --out icon.iconset/icon_512x512.png
sips -z 1024 1024 icon.png --out icon.iconset/icon_512x512@2x.png

# 转换为 .icns
iconutil -c icns icon.iconset -o build/icon.icns
```

## 使用图标

1. 将生成的 `icon.icns` 文件放在 `build/` 目录下
2. 重新打包：
   ```bash
   npm run build:dmg
   ```

## 注意事项

- 图标必须是正方形
- 建议使用透明背景的 PNG
- 图标设计要简洁，避免过多细节
- 确保图标在小尺寸（16×16）下也能清晰识别

